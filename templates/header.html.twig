


<!-- banner -->
	<div class="banner">
		<div class="container">
			<nav class="navbar navbar-default">
				<div class="navbar-header navbar-left">
					<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
						<span class="sr-only">Toggle navigation</span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</button>
					<h1><a class="navbar-brand" href="{{ path('home') }}">Sym<span>phony</span></a></h1>
				</div>
				<!-- Collect the nav links, forms, and other content for toggling -->
				<div class="collapse navbar-collapse navbar-right" id="bs-example-navbar-collapse-1">
					<nav class="menu menu--iris">
						<ul class="nav navbar-nav menu__list">
							<li class="menu__item"><a href="{{ path('home') }}" class="menu__link">Home</a></li>
							<li class="menu__item"><a href="{{ path('artiste_index') }}" class="menu__link">Artistes</a></li>
							<li class="menu__item"><a href="{{ path('album_index') }}" class="menu__link">Albums</a></li>
							<li class="menu__item"><a href="{{ path('categorie_index') }}" class="menu__link">Categories</a></li>
							{% if is_granted('ROLE_USER')==false %}
							<li class="menu__item "><a href="{{ path('app_login') }}" class="menu__link">Login</a></li>
							<li class="menu__item"><a href="{{ path('app_register') }}" class="menu__link">Register</a></li>
											{% endif %}

                                    {% if is_granted('ROLE_USER') %}
								<li class="menu__item "><a href="{{ path('favoris_index') }}" class="menu__link">Favoris</a></li>
                            	<li class="dropdown menu__item">
								<a class="dropdown-toggle menu__link" data-toggle="dropdown"><a href="{{ path('playlist_index') }}">Playlistes <b class="caret"></b></a>
								<ul class="dropdown-menu agile_short_dropdown">
                                            {% for playlist in play %}

									<li><a href="/playlist/{{playlist.id}}">{{playlist.nom}}</a></li>
									{% endfor %}
								</ul>
							</li>
                              
							<li class="menu__item"><a href="/user/{{user.id}}" class="menu__link">{{user.email}}</a></li>
                            {% endif %}
						</ul>
					</nav>
				</div>
			</nav>
		
<!-- //banner -->	
